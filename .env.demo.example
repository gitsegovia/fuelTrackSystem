# Copiar a .env.demo y completar.
# Usar junto con: docker compose --env-file .env.demo -f docker-compose.yml -f docker-compose.demo.yml up --build
DB_USER=postgres
DB_PASS=GENERATE_A_STRONG_PASSWORD
DB_DATABASE=fueltrack
DB_HOST_PORT=5433
API_PORT=4000
WEB_PORT=3000
